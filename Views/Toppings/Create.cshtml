@model Pizza_Shop.Models.Topping

@{
    ViewData["Title"] = "Create";
}

<h1>New Topping</h1>

<h4><b>What ingredient would you like to add for our chefs?</b></h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ToppingName" class="control-label">Name</label>
                <input asp-for="ToppingName" class="form-control" />
                <span asp-validation-for="ToppingName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
